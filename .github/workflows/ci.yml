name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
#  lint:
#    runs-on: ubuntu-latest
#
#    steps:
#    - uses: actions/checkout@v2
#
#    - name: Set up Python
#      uses: actions/setup-python@v2
#      with:
#        python-version: 3.9  # You can choose the version you want here
#
#    - name: Install Poetry
#      run: |
#        curl -sSL https://install.python-poetry.org | python3 -
#
#    - name: Install dependencies
#      run: |
#        poetry install
#
#    - name: Run Flake8
#      run: |
#        poetry run flake8 mentor_mingle  # Replace 'mentor_mingle' with your module name or path if different
#
#    - name: Run Pylint
#      run: |
#        poetry run pylint mentor_mingle

  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.10', '3.11']

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Poetry
      uses: snok/install-poetry@v1
    - name: Install dependencies
      run: |
        poetry install
    - name: Run Flake8
      run: |
        poetry run flake8 --max-line-length=120 mentor_mingle
    - name: Run Tests
      run: |
        poetry run pytest
